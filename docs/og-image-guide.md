# OG Image

The Open Graph image is a static file served from `public/`.

## Requirements

- Size: 1200x630 (recommended)
- Format: PNG

## Current Asset

- `public/og-image-v3.png`

## Where It Is Wired

- `src/app/layout.tsx` sets `openGraph.images` and `twitter.images` via `ogImageUrl`.
